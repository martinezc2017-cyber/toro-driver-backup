============================================
INFORMACION NECESARIA DEL APP CLIENTE
============================================

Por favor copia y pega la siguiente informacion del proyecto CLIENTE:

1. SUPABASE CONFIG
   - Archivo: lib/src/config/supabase_config.dart (o similar)
   - Necesito ver:
     * supabaseUrl
     * Nombre de la tabla de rides (ridesTable, ride_requests, etc.)

2. SERVICIO DE RIDES
   - Archivo donde se CREA el viaje (ride_service.dart o similar)
   - Necesito ver:
     * Nombre de la tabla donde inserta el viaje
     * Campos que envia (status, passenger_id, pickup_*, dropoff_*, etc.)
     * Valor del campo 'status' cuando crea el viaje

3. VERIFICAR EN SUPABASE DASHBOARD
   - Ve a: https://supabase.com/dashboard
   - Proyecto > Table Editor > rides (o el nombre de tu tabla)

   PREGUNTAS:
   a) Existe la tabla 'rides'? Si no, como se llama?
   b) Hay registros con status = 'pending'?
   c) El campo se llama 'status' o 'ride_status'?

   - Ve a: Database > Replication
   d) La tabla de rides tiene Realtime habilitado? (debe estar en la lista)

============================================
PEGA LA INFORMACION AQUI ABAJO:
============================================




