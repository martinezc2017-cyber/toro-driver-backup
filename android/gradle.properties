org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=1G -XX:ReservedCodeCacheSize=256m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8
android.useAndroidX=true

# Mapbox Navigation SDK
# El token SECRETO (sk.) ya NO vive aquí (se filtraba en el repo). Se lee de:
#   ~/.gradle/gradle.properties (nivel usuario, fuera del repo)  o  env var MAPBOX_DOWNLOADS_TOKEN
# Misma cuenta Mapbox que el rider (martinezc2017). Rotar en account.mapbox.com.
MAPBOX_DOWNLOADS_TOKEN=REPLACE_IN_USER_GRADLE_PROPERTIES
